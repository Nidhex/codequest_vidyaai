{
  "name": "vidya-ai",
  "version": "1.0.0",
  "description": "Vidya AI: India's Next-Generation Multilingual AI Learning Platform",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "start:rag": "cd rag-service && python -m uvicorn main:app --host 127.0.0.1 --port 8000",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\" \"npm run start:rag\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
